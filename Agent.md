# Agent.md

## 项目定位

- 本仓库用于构建一个以 `美食` 为主题的 `小游戏网站`。
- 这不是个人主页，不是品牌官网，也不是纯内容展示站。
- 这是一个强调 `好玩、好吃、轻互动、易传播` 的休闲游戏项目。
- 站点应优先传达“看到就想点一下、玩一下、分享一下”的体验，而不是信息陈列。

## 核心方向

- 轻游戏化：交互要简单直接，用户进入后能立刻开始玩。
- 强美食感：视觉、文案、反馈都要围绕食物、料理、点单、收集、挑战等主题展开。
- 轻门槛：规则易懂，不依赖复杂教程。
- 高反馈：点击、拖拽、得分、通关、失败都要有明确反馈。
- 易传播：适合做成一眼能懂、几秒上手的小玩法。

## 明确禁止

- 禁止做成个人主页、作品集、博客页、企业官网或普通落地页。
- 禁止页面主要内容变成长篇介绍、项目说明、团队介绍或品牌故事。
- 禁止使用与美食主题无关的抽象概念堆砌页面。
- 禁止为了凑内容加入假功能、假排行榜、假评论、假社交数据。
- 禁止复杂到需要多层教学的新手引导。
- 禁止使用廉价“儿童小游戏”风格的混乱高饱和配色和劣质素材拼贴。

## 内容原则

- 首页以 `开始游戏`、`玩法说明`、`当前目标` 为核心。
- 文案应短、直接、俏皮，服务于操作和反馈。
- 内容结构优先围绕 `开始`、`规则`、`挑战`、`得分`、`奖励`、`再来一局` 展开。
- 若后续增加内容，应优先扩展关卡、玩法、食物主题、成就系统，而不是扩展资讯栏目。
- 所有内容必须服务于统一命题：`美食主题小游戏`。

## 视觉方向

- 总体风格：鲜明、轻松、好吃、可爱但不过度幼稚。
- 目标体验：像一个设计干净、反馈爽快的美食互动小游乐场。
- 页面应突出食物主体，减少与玩法无关的装饰。
- 排版应服务于操作效率，保证按钮、分数、状态一眼可见。

## 固定色板

以下颜色作为本项目当前默认配色，未经明确决策不要擅自更换：

```css
:root {
  --tomato: #e4572e;
  --orange-peel: #f3a712;
  --cream: #fff7e8;
  --matcha: #6a994e;
  --berry: #b56576;
  --chocolate: #4a2f27;
  --text: #2f241f;
  --text-muted: #7a685f;
  --line: rgba(74, 47, 39, 0.12);
}
```

## 配色使用规则

- 背景主色优先使用 `--cream`。
- 主按钮、核心行动点优先使用 `--tomato`。
- 强调分数、奖励、特殊状态可使用 `--orange-peel`。
- 成功态、健康食材、正向反馈可使用 `--matcha`。
- 点缀色可使用 `--berry`，但不能压过主玩法。
- 深色文字和结构线优先使用 `--chocolate`。

## 设计限制

- 不允许页面出现传统 SaaS Hero 区、商务按钮组或品牌宣讲式布局。
- 不允许默认科技风蓝紫渐变。
- 不允许用过多浮夸动画干扰操作。
- 不允许大量无意义卡片、图标墙、说明墙占据首屏。
- 不允许把游戏做成“看起来像游戏，实际上不能快速开始”的假交互页面。

## 允许的视觉元素

- 食物插画或食材图形。
- 清晰的分数和状态模块。
- 有节奏的按钮反馈。
- 轻量弹跳、缩放、掉落、收集类动效。
- 桌布、菜单板、餐盘、厨房台面等与美食相关的界面隐喻。
- 适度纹理和阴影，用于增强食物层次感。

## 开发原则

1. 先保证玩法成立，再补视觉包装。
2. 每次改版都必须检查是否仍然像“美食小游戏”，而不是普通展示页。
3. 如果某个设计方案削弱了可玩性，应直接否决。
4. 如果某个模块不能提升理解、操作或反馈，应删除，不要硬凑。
5. 若需要新增页面或模块，先说明它如何服务于玩法闭环。

## 交付要求

- 说明本次新增或修改服务于什么玩法目标或视觉目标。
- 说明是否符合当前 `Agent.md` 的约束。
- 若存在妥协，必须明确指出妥协点，不得隐瞒。
